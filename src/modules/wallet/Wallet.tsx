import React from 'react';
import { View, Text, StyleSheet } from 'react-native';
import DefaultOverlay from '../../common/components/DefaultOverlay';

const Wallet = () => {
  return (
    <DefaultOverlay text={"Wallet"} />
  );
};


export default Wallet;
